package revolhope.splanes.com.core.interactor
/*

import revolhope.splanes.com.core.domain.repositoryimpl.SerieRepositoryImpl
import revolhope.splanes.com.core.domain.model.Serie

class AddSerieUseCase(private val serieRepositoryImpl: SerieRepositoryImpl) {

    suspend operator fun invoke(

        serie: Serie,
        onSuccess: (Boolean) -> Unit,
        onFailure: (throwable: Throwable) -> Unit

    ) {
        serieRepositoryImpl.insert(serie, onSuccess, onFailure)
    }

}*/
